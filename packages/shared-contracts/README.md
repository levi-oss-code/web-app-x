# Shared Contracts

Single source of truth for:

- API request/response schemas
- Shared entity types
- Error code constants

Both frontend and backend should import from this package to avoid contract drift.
